<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- The LiveKit call page asks for these through the WebView. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Android 13+: the user must grant notifications before FCM can show any. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- A teacher ringing a student has to behave like a phone call: wake the
         screen, ring over the lock screen, show a call UI rather than a tray
         item. Android 14 restricts USE_FULL_SCREEN_INTENT to calling and alarm
         apps — sideloading is fine; a Play Store listing needs a declaration. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <!-- Screen sharing. Android WebView has no getDisplayMedia, so presenting
         from the app is native capture (MediaProjection) published into the
         same LiveKit room — and the platform refuses to hand out a projection
         unless a foreground service of this exact type is already running. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>

    <application
        android:label="Novice Tutor"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- supportsPictureInPicture: shrinks the class into a floating window
             when the student switches apps mid-lesson, instead of leaving them
             with audio and no picture. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- novicetutor.com links open in the app rather than the browser.
                 android:autoVerify needs /.well-known/assetlinks.json served by
                 the site with this app's signing fingerprint; until that exists
                 Android shows a chooser instead, which still works. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="novicetutor.com"/>
                <data android:scheme="https" android:host="www.novicetutor.com"/>
            </intent-filter>
        </activity>
        <!-- Holds the screen capture up while a teacher presents. Not exported:
             nothing outside the app has any business starting it. -->
        <service
            android:name=".ScreenShareService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"/>

        <!-- FCM posts to this channel; the id must match the one the server
             sends in android.notification.channelId (src/lib/fcm.ts). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="novice_tutor_default" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11 package visibility: without this, url_launcher cannot
             see a browser and off-site links (Google OAuth, Stripe) silently
             fail to open. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
