{
  "name": "quran-lms-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@10.0.0",
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
